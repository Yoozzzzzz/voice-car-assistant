# 车机语音问答助手 - 项目文档

## 一、项目概述

### 1.1 项目定位

一款运行在 **后装 Android 车机** 上的语音问答软件，车主通过语音提问，系统实时调用国内免费大模型生成回答，并通过语音播报回复。

### 1.2 核心功能

| 功能 | 说明 |
|------|------|
| 语音采集 | 实时采集车内麦克风 PCM 音频流 |
| 语音识别（ASR） | 将音频流转换为文字 |
| 大模型问答 | 调用免费大模型 API 生成回复 |
| 语音合成（TTS） | 将文字回复转换为语音播报 |
| 流式对话 | 支持多轮连续对话，无需反复唤醒 |

### 1.3 非功能需求

| 指标 | 目标值 |
|------|--------|
| 端到端延迟 | ≤ 3 秒（从说话结束到语音播报） |
| 识别准确率 | 车载噪声环境下 ≥ 90% |
| 断线重连 | WebSocket 断开后 5 秒内自动重连 |
| 内存占用 | 车机端 APK 运行时 ≤ 200MB |

---

## 二、技术架构

### 2.1 整体架构图

```
┌──────────────────────────────────────────────────────┐
│                  Android 车机端（APK）                  │
│  ┌─────────────┐  ┌──────────────┐  ┌─────────────┐  │
│  │ 音频采集模块  │  │  WebSocket  │  │ 音频播放模块  │  │
│  │ (AudioRecord)│◄─┤   客户端     ├─►│(TrackPlayer) │  │
│  └──────┬──────┘  └──────┬───────┘  └─────────────┘  │
│         │                │                            │
│         │   PCM 音频流    │   TTS 音频流                │
│         ▼                ▼                            │
│  ┌─────────────────────────────────────────────────┐  │
│  │           VAD 语音活动检测（自动断句）             │  │
│  └─────────────────────────────────────────────────┘  │
└──────────────────────────┬─────────────────────────────┘
                           │ WebSocket (wss://)
                           │ 全双工流式传输
                           ▼
┌──────────────────────────────────────────────────────┐
│              Node.js 后端服务（云服务器）               │
│  ┌─────────────┐  ┌──────────────┐  ┌─────────────┐  │
│  │  WebSocket  │  │   ASR 服务    │  │   TTS 服务   │  │
│  │   服务端     │◄─┤ (讯飞/阿里云) │  │ (讯飞/Edge) │  │
│  └──────┬──────┘  └──────┬───────┘  └──────┬──────┘  │
│         │                │                  │         │
│         │   识别文本      │                  │ 合成音频 │
│         ▼                ▼                  ▼         │
│  ┌─────────────────────────────────────────────────┐  │
│  │         LLM 大模型服务（智谱 GLM-4.7-Flash）       │  │
│  │         流式返回文本 → 边接收边合成 TTS            │  │
│  └─────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────┘
```

### 2.2 技术选型

| 模块 | 技术方案 | 选型理由 |
|------|---------|---------|
| 前端框架 | React Native + Expo | 复用前端技术栈，跨平台，热重载调试 |
| 音频采集 | react-native-audio-record | 原生 PCM 采集，支持 16kHz/16bit |
| 音频播放 | react-native-track-player | 支持后台播放、音频焦点管理 |
| WebSocket | react-native-ws / ws | 全双工流式通信，低延迟 |
| 后端框架 | Node.js + Express | 生态成熟，npm 包丰富 |
| ASR | 讯飞语音识别 SDK | 中文识别率高，车载降噪算法成熟 |
| LLM | 智谱 GLM-4.7-Flash（默认）/ 豆包 Seed 2.0 mini（可选） | GLM-4.7-Flash **永久免费**，200K 上下文，兼容 OpenAI 接口；豆包 doubao-seed-2-0-mini-260428 由火山方舟提供，同为 OpenAI 兼容接口，可通过 `LLM_PROVIDER` 切换 |
| TTS | 讯飞语音合成 / Edge TTS（备用） | 中文自然度高，Edge 完全免费 |

### 2.3 目录结构

```
voice-car-assistant/
├── client/                          # React Native 车机端
│   ├── src/
│   │   ├── components/              # UI 组件
│   │   │   ├── ChatBubble.tsx       # 对话气泡
│   │   │   ├── VoiceWave.tsx        # 语音波形动画
│   │   │   └── SettingsPanel.tsx    # 设置面板
│   │   ├── services/                # 业务服务
│   │   │   ├── audioService.ts      # 音频采集/播放
│   │   │   ├── websocketService.ts  # WebSocket 连接管理
│   │   │   └── vadService.ts        # VAD 语音活动检测
│   │   ├── hooks/                   # 自定义 Hooks
│   │   │   ├── useVoiceAgent.ts     # 语音代理逻辑
│   │   │   └── useAutoReconnect.ts  # 自动重连
│   │   ├── store/                   # 状态管理
│   │   │   └── conversationStore.ts # 对话状态
│   │   └── App.tsx                  # 入口
│   ├── app.json
│   └── package.json
│
├── server/                          # Node.js 后端服务
│   ├── src/
│   │   ├── websocket/               # WebSocket 服务
│   │   │   ├── server.ts            # WS 入口
│   │   │   └── handler.ts           # 消息处理器
│   │   ├── asr/                     # 语音识别
│   │   │   └── xunfeiAsr.ts         # 讯飞 ASR
│   │   ├── llm/                     # 大模型
│   │   │   └── zhipuClient.ts       # 智谱 GLM 客户端
│   │   ├── tts/                     # 语音合成
│   │   │   ├── xunfeiTts.ts         # 讯飞 TTS
│   │   │   └── edgeTts.ts           # Edge TTS（备用）
│   │   ├── prompts/                 # 提示词加载器（读 prompts/*.md）
│   │   ├── utils/                   # 工具函数
│   │   │   ├── audioConverter.ts    # 音频格式转换
│   │   │   └── logger.ts            # 日志
│   │   └── index.ts                 # 服务入口
│   ├── prompts/                     # 提示词 md（system-prompt.md，可直接编辑）
│   ├── .env                         # 环境变量（API Key）
│   └── package.json
│
└── docs/                            # 文档
    ├── API.md                       # 接口文档
    └── DEPLOY.md                    # 部署指南
```

---

## 三、核心模块设计

### 3.1 音频采集模块（client）

**职责**：采集车内麦克风 PCM 音频流，实时通过 WebSocket 推送至后端。

**关键参数**：
```typescript
const AUDIO_CONFIG = {
  sampleRate: 16000,      // 采样率 16kHz（ASR 标准）
  channels: 1,            // 单声道
  bitsPerSample: 16,      // 16bit
  bufferSize: 4096,       // 缓冲区大小
  format: 'PCM',          // 原始 PCM 格式
};
```

**核心流程**：
```
初始化 AudioRecord → 开始采集 → 每 200ms 推送一个 PCM chunk
→ 检测 VAD（静音超时 800ms 判定为说话结束）→ 发送结束标记
```

### 3.2 WebSocket 通信协议

**连接地址**：`wss://your-domain.com/ws`

**消息格式**（JSON）：

```typescript
// 客户端 → 服务端
interface ClientMessage {
  type: 'audio' | 'text' | 'control';
  data: string | ArrayBuffer;    // audio: PCM base64 / text: 文本
  sessionId: string;             // 会话 ID
  timestamp: number;
}

// 服务端 → 客户端
interface ServerMessage {
  type: 'asr_result' | 'llm_chunk' | 'llm_end' | 'llm_retry' | 'tts_audio' | 'conversation_reset' | 'pong' | 'error';
  // asr: 识别文本 / llm_chunk: 流式文本 / llm_end: 完整回复 / llm_retry: 重试进度(retry/maxRetries)
  // tts: 音频 base64 / pong: 心跳响应 / error: 错误码+原因
  data: string | ArrayBuffer;
  sessionId: string;
  isFinal: boolean;              // 是否为最终结果
}
```

**会话流程**：
```
1. 客户端发送 { type: 'audio', data: PCM_chunk }
2. 服务端返回 { type: 'asr_result', data: "今天天气怎么样" }
3. 服务端调用 LLM，流式返回 { type: 'llm_chunk', data: "今天..." }
4. 服务端同步合成 TTS，返回 { type: 'tts_audio', data: audio_base64 }
5. 客户端播放音频，完成一轮对话
```

### 3.3 大模型服务（server）

**多供应商支持**：`LLM_PROVIDER=zhipu|doubao` 切换（默认 zhipu），两者均为 OpenAI 兼容接口：

| 供应商 | baseURL | 模型 | 鉴权 |
|--------|---------|------|------|
| 智谱（默认） | `https://open.bigmodel.cn/api/paas/v4/` | `glm-4.7-flash` | `ZHIPU_API_KEY` |
| 火山方舟豆包 | `https://ark.cn-beijing.volces.com/api/v3` | `doubao-seed-2-0-mini-260428` | `DOUBAO_API_KEY`（ARK_API_KEY） |

两者均为深度思考模型且**默认开启思考**，思考与正文共用 `max_tokens`，本服务显式传 `thinking: {type:'disabled'}` 关闭以保证首字延迟。



**智谱 GLM-4.7-Flash 调用封装**：

```typescript
import OpenAI from 'openai';

const zhipuClient = new OpenAI({
  apiKey: process.env.ZHIPU_API_KEY,
  baseURL: 'https://open.bigmodel.cn/api/paas/v4/',
});

async function chatStream(messages: Array<{role: string, content: string}>) {
  const stream = await zhipuClient.chat.completions.create({
    model: 'glm-4.7-flash',
    messages: [
      { role: 'system', content: '你是车载语音助手，回答简洁口语化，不超过50字' },
      ...messages,
    ],
    stream: true,
    max_tokens: 200,
  });

  for await (const chunk of stream) {
    const content = chunk.choices[0]?.delta?.content;
    if (content) yield content;  // 流式返回
  }
}
```

**系统 Prompt 设计**：
```
你是车载语音助手，请遵守以下规则：
1. 回答简洁口语化，单轮回复不超过 50 字
2. 优先回答与驾驶相关的问题（导航、天气、车辆状态）
3. 不涉及政治、暴力、成人内容
4. 无法回答时，礼貌告知并建议手动查询
```

### 3.4 VAD 语音活动检测

**目的**：自动检测车主说话结束，无需手动按按钮。

**算法逻辑**：
```typescript
class VadService {
  private silenceThreshold = -40;  // dB，低于此值判定为静音
  private silenceDuration = 800;   // ms，连续静音 800ms 判定为说话结束
  private silenceTimer: NodeJS.Timeout | null = null;

  onAudioChunk(pcmData: Int16Array): boolean {
    const energy = this.calculateEnergy(pcmData);
    const db = 20 * Math.log10(energy / 32768);

    if (db < this.silenceThreshold) {
      // 静音中，启动计时器
      if (!this.silenceTimer) {
        this.silenceTimer = setTimeout(() => {
          this.onSpeechEnd();  // 触发说话结束事件
        }, this.silenceDuration);
      }
    } else {
      // 有声音，清除计时器
      if (this.silenceTimer) {
        clearTimeout(this.silenceTimer);
        this.silenceTimer = null;
      }
    }
    return db >= this.silenceThreshold;  // 是否为有效语音
  }
}
```

### 3.5 系统提示词（可编辑，外置 md）

**文件**：`server/prompts/system-prompt.md`（2026-09-23 起由硬编码常量外置为 md，改提示词**不用改代码**）

**加载机制**：
- 服务端启动时读取一次（`src/prompts/systemPrompt.ts`），修改 md 后**重启服务**生效
- 解析规则：普通段落与 `- ` 列表项会发送给模型（列表符号去掉、逐行拼接）；标题（`#`）、HTML 注释、引用块（`>` 行首）**不会发送**，可用于写给人看的说明
- 路径覆盖：环境变量 `SYSTEM_PROMPT_FILE`（绝对路径，或相对 server 启动目录）；默认 `server/prompts/system-prompt.md`
- 容错：文件缺失/解析为空 → 回退内置兜底提示词 + 打 warn 日志，不中断 LLM 链路
- 自检：`GET /health` 的 `systemPrompt` 字段返回 `{ source, chars, usedFallback }`，可确认当前加载的是哪份提示词

**编写约束**（车机语音场景）：
1. 口语化短句——短句显著降低 TTS 首句延迟；
2. 句末标点必须规范（，。！？）——T1.7 按标点切分触发 TTS 合成；
3. 禁 emoji / markdown / 列表 / 代码块——语音播报不友好。

> 原硬编码位置：`server/src/services/llmService.ts` 的 `SYSTEM_PROMPT` 常量（已改为从 md 加载）。

---

## 四、部署方案

### 4.1 后端部署

**云服务器配置建议**：
| 配置项 | 推荐值 |
|--------|--------|
| CPU | 2 核 |
| 内存 | 4GB |
| 带宽 | 5Mbps（音频流传输） |
| 系统 | Ubuntu 22.04 LTS |

**部署步骤**：
```bash
# 1. 安装 Node.js 20 LTS
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# 2. 克隆项目
git clone https://github.com/your-repo/voice-car-assistant.git
cd voice-car-assistant/server

# 3. 安装依赖
npm install

# 4. 配置环境变量
cp .env.example .env
# 编辑 .env，填入智谱 API Key、讯飞 AppID 等

# 5. 启动服务（生产环境用 PM2）
npm install -g pm2
pm2 start src/index.ts --name voice-server
pm2 save
pm2 startup
```

**Nginx 反向代理（HTTPS）**：
```nginx
server {
    listen 443 ssl;
    server_name your-domain.com;

    ssl_certificate /etc/ssl/certs/your-cert.pem;
    ssl_certificate_key /etc/ssl/private/your-key.pem;

    location /ws {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 86400;
    }
}
```

### 4.2 车机端部署

**方式一：Expo Go 快速测试**
```bash
cd client
npm install
npx expo start
# 扫码在车机上用 Expo Go 运行
```

**方式二：打包 APK 安装**
```bash
# 1. 预构建
npx expo prebuild --platform android

# 2. 生成 APK
cd android
./gradlew assembleRelease

# 3. 拷贝 APK 到 U 盘，插入车机安装
```

**车机权限配置**（AndroidManifest.xml）：
```xml
<uses-permission android:name="android.permission.RECORD_AUDIO" />
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
```

---

## 五、API Key 获取指南

### 5.1 智谱 GLM（大模型）

1. 访问 [智谱开放平台](https://open.bigmodel.cn/)
2. 注册账号 → 实名认证
3. 进入「API Keys」页面 → 创建新 Key
4. GLM-4.7-Flash **永久免费**，无 Token 限制

### 5.2 讯飞开放平台（ASR + TTS）

1. 访问 [讯飞开放平台](https://www.xfyun.cn/)
2. 注册账号 → 实名认证
3. 创建应用 → 开通「语音听写」和「在线语音合成」服务
4. 获取 AppID、APISecret、APIKey

**免费额度**：
- 语音听写：新用户 500 小时免费
- 语音合成：新用户 500 小时免费

### 5.3 Edge TTS（备用，完全免费）

无需 API Key，直接调用微软 Edge 浏览器的 TTS 接口，中文音色自然度高。

### 5.4 火山方舟豆包（可选 LLM）

1. 访问 [火山方舟控制台](https://console.volcengine.com/ark/region:cn-beijing/apikey) → 创建 API Key
2. **必须**在[「开通管理」](https://console.volcengine.com/ark/region:cn-beijing/openManagement)中开通 Doubao-Seed 模型（如 doubao-seed-2-0-mini）
   - 方舟要求**逐个模型显式开通**，仅有 API Key 会报 `ModelNotOpen`：`Your account xxx has not activated the model xxx. Please activate the model service in the Ark Console`
   - 开通免费，个人用户享免费额度（如 Doubao-Seed 系列试用额度）；超出免费额度后才按量计费
3. 在 `server/.env` 中设置：`LLM_PROVIDER=doubao` + `DOUBAO_API_KEY=<你的 Key>`
4. 重启服务，访问 `GET /health` 的 `llm` 字段确认已切换

> 也可在方舟控制台创建推理接入点（Endpoint），将 `DOUBAO_MODEL` 填为 `ep-xxxxxxxx` 形式。

**常见方舟报错对照**

| 报错 | 含义 | 处理 |
|------|------|------|
| `ModelNotOpen` / has not activated the model | 模型未开通 | 控制台「开通管理」开通该模型 |
| `has exhausted the free trial quota` | 免费额度用尽 | 控制台开启按量计费/充值，或切回 zhipu |
| `has reached the set inference limit ... paused` | 触达用量上限被暂停 | 控制台调整调用量上限或充值 |
| 429 rate limit | 并发/速率限流 | 服务端已自动指数退避重试 3 次 |

---

## 六、风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|---------|
| 车机网络不稳定 | WebSocket 断连，对话中断 | 前端自动重连 + 本地缓存未发送的音频 |
| 免费 API 限流 | 高峰期请求被拒绝 | 多 Key 轮询 + 本地降级（返回预设回复） |
| 车载噪声识别率低 | ASR 识别错误 | 车内麦克风已有降噪 + 讯飞车载专用模型 |
| API Key 泄露 | 被他人盗用产生费用 | Key 仅存后端环境变量，前端通过 Token 鉴权 |
| 车机内存不足 | APK 崩溃 | 音频流分块传输，不在内存中累积完整音频 |

---

## 七、开发排期建议

| 阶段 | 任务 | 预计工时 |
|------|------|---------|
| Week 1 | 后端 WebSocket 服务 + 智谱 LLM 对接 | 3 天 |
| Week 1 | 讯飞 ASR/TTS 对接 + 流式处理 | 2 天 |
| Week 2 | React Native 音频采集 + VAD 实现 | 3 天 |
| Week 2 | WebSocket 客户端 + 音频播放 | 2 天 |
| Week 3 | UI 界面 + 对话状态管理 | 2 天 |
| Week 3 | 联调测试 + 延迟优化 | 3 天 |
| Week 4 | 车机实车测试 + Bug 修复 | 5 天 |

**总计**：约 4 周可完成 MVP 版本

---

## 八、后续扩展方向

1. **离线降级**：弱网时切换本地关键词识别（如"打开空调"、"导航回家"）
2. **车辆控制**：对接车机 CAN 总线或车企开放 API，实现语音控车
3. **多模态交互**：结合车机屏幕，语音 + 触控混合交互
4. **个性化记忆**：记录车主偏好（常去地点、音乐喜好），提供更智能的推荐


